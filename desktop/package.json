{
  "name": "wlog-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "wlog desktop app — wraps the embedded wlog UI via Tauri, bundling wlog.exe as a sidecar.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
