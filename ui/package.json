{
  "name": "wlog-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "wlog embedded static UI (Vanilla TS + Vite + uPlot)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "uplot": "1.6.32"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vite": "5.4.11"
  }
}
